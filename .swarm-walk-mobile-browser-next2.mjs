import puppeteer from 'puppeteer-core';
const BASE = 'http://localhost:8963';
const OUT = '/private/tmp/claude-501/-Users-sindri-Documents-Website-redesign-mockups/dda2a378-fed2-4edc-83a5-2a93b4e70d42/scratchpad/swarm/walk-mobile-browser';
const br = await puppeteer.launch({ executablePath: '/Applications/Google Chrome.app/Contents/MacOS/Google Chrome', headless: 'new' });
const page = await br.newPage();
await page.setViewport({ width: 390, height: 844, deviceScaleFactor: 1, isMobile: true, hasTouch: true });
await page.goto(BASE + '/verkefni/fjallalind', { waitUntil: 'networkidle2' });
await new Promise(r => setTimeout(r, 1200));
const nextEl = await page.$('a.ki-proj-adj-link--on');
await nextEl.evaluate(el => el.scrollIntoView({ block: 'center' }));
await new Promise(r => setTimeout(r, 300));
await page.screenshot({ path: `${OUT}/10-next-project-card.png` });
const box = await nextEl.boundingBox();
console.log('NEXT_CARD_BOX=' + JSON.stringify(box));
await nextEl.click();
await new Promise(r => setTimeout(r, 1000));
console.log('LANDED=' + page.url());
await page.screenshot({ path: `${OUT}/11-next-project-landed.png` });
await br.close();
